Write a file, creating or overwriting it

Use this to create the data-description file (typically
`.querychat/<table>.md`) or to overwrite it wholesale. Parent directories are
created automatically.

Files are restricted to the current working directory. Prefer `edit` over
`write` once the file exists, so you change only what needs changing and the
analyst can follow along.

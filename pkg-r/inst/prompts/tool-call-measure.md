Execute a single trusted measure and return the result.

The result may be a scalar value or a markdown table. Use the name and argument schema returned by `querychat_search_measures`. To visualize a data frame result, use the `querychat_run_measures` → `querychat_visualize_measures` pipeline instead.

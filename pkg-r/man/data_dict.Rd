% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DataDict.R
\name{data_dict}
\alias{data_dict}
\alias{table_spec}
\alias{column_spec}
\alias{column_range}
\alias{relationship_spec}
\title{Data Dictionary for Rich Table Metadata}
\usage{
data_dict(
  name = NULL,
  description = NULL,
  tables = list(),
  relationships = list(),
  glossary = list()
)

table_spec(description = NULL, details = NULL, columns = list())

column_spec(
  name,
  type = NULL,
  constraints = character(),
  description = NULL,
  details = NULL,
  units = NULL,
  values = NULL,
  range = NULL,
  examples = NULL
)

column_range(min = NULL, max = NULL)

relationship_spec(join, description = NULL, cardinality = NULL)
}
\arguments{
\item{name}{Column name as it appears in the data source.}

\item{description}{Human-readable description of the relationship.}

\item{tables}{Named list of \code{\link[=table_spec]{table_spec()}} objects, keyed by table name.
Table names must match those registered with \code{QueryChat}.}

\item{relationships}{List of \code{\link[=relationship_spec]{relationship_spec()}} objects declaring
cross-table relationships.}

\item{glossary}{Named list or character vector of domain term definitions
(e.g. \code{list(ARR = "Annual Recurring Revenue")}).}

\item{details}{Longer narrative about the column, used only in the on-demand
\code{get_schema} tool response.}

\item{columns}{List of \code{\link[=column_spec]{column_spec()}} objects. Columns not listed here are
documented using live statistics inferred from the data.}

\item{type}{Human-readable type override (e.g. \code{"date"}, \code{"currency"}).
When supplied, this replaces the inferred SQL type in the LLM schema view.}

\item{constraints}{Free-text constraints conveyed to the LLM
(e.g. \code{"non-negative"}). A character vector; defaults to \code{character()}.}

\item{units}{Unit label (e.g. \code{"kg"}, \code{"USD"}), included in the schema view.}

\item{values}{Exhaustive list of valid values. Replaces categorical inference
for this column — querychat will not query the data source for distinct
values when this is set.}

\item{range}{A \code{\link[=column_range]{column_range()}} object giving inclusive min/max bounds.
Replaces live min/max statistics queries when set.}

\item{examples}{Representative sample values shown to the LLM as context.}

\item{min}{Minimum value (inclusive).}

\item{max}{Maximum value (inclusive).}

\item{join}{SQL JOIN clause or expression that links the tables
(e.g. \code{"orders.customer_id = customers.id"}).}

\item{cardinality}{Cardinality string (e.g. \code{"one-to-many"}).}
}
\value{
A \code{querychat_data_dict} S3 object.

A \code{querychat_table_spec} S3 object.

A \code{querychat_column_spec} S3 object.

A \code{querychat_column_range} S3 object.

A \code{querychat_relationship_spec} S3 object.
}
\description{
A data dictionary provides rich per-table and per-column metadata to the LLM,
allowing you to document tables and columns without relying entirely on
live statistics inferred from the data source.

For columns listed in a data dictionary:
\itemize{
\item \code{values} replaces categorical inference (no \verb{SELECT DISTINCT} query).
\item \code{range} replaces live min/max statistics queries.
\item \code{description} is forwarded verbatim to the LLM's schema view.
}

Columns not listed fall back to the normal live-statistics path.
}
\section{Functions}{
\itemize{
\item \code{table_spec()}: Per-table metadata.

\item \code{column_spec()}: Per-column metadata.

\item \code{column_range()}: Inclusive numeric range for a column.

Pass to the \code{range} argument of \code{\link[=column_spec]{column_spec()}} to override live min/max
statistics queries.

\item \code{relationship_spec()}: A declared relationship between two tables.

}}

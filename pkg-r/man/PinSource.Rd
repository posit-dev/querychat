% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PinSource.R
\name{PinSource}
\alias{PinSource}
\title{Pin Source}
\description{
A DataSource implementation that reads data from a
\href{https://pins.rstudio.com/}{pins} board. When the \code{"duckdb"} engine is used
and the pin type is one DuckDB can read natively (parquet, CSV, JSON), the
data is loaded directly from the cached pin files into DuckDB without
deserializing into R. For other pin types (e.g. RDS), or when the \code{"sqlite"}
engine is used, the data is deserialized via \code{pin_read()} and must produce
a data frame (or tibble), which is then registered with the chosen engine
just like \link{DataFrameSource}.

When loaded into DuckDB, the connection's external file access is locked
down so that LLM-generated SQL cannot reach the filesystem.

If the pin has a title, description, or tags, \link{QueryChat} uses them as
the default \code{data_description}, which you can override.
}
\section{Lazy queries with pins}{


\code{PinSource} materializes the full dataset into DuckDB. For large parquet
pins where you want lazy query execution, read the pin files yourself and
pass a \code{tbl_sql} to \code{\link[=querychat]{querychat()}} instead:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{paths <- pins::pin_download(board, "my_pin")
con <- DBI::dbConnect(duckdb::duckdb())
DBI::dbExecute(
  con,
  sprintf("CREATE VIEW my_pin AS SELECT * FROM read_parquet('\%s')", paths[1])
)
qc <- querychat(dplyr::tbl(con, "my_pin"))
}\if{html}{\out{</div>}}

The pin files are still downloaded to a local cache --- \code{pin_download()}
always fetches them. But rather than loading everything into memory, DuckDB
reads the parquet file lazily through dbplyr.

This approach skips the security lockdown that \code{PinSource} applies, so
LLM-generated SQL can access files on the local system.
}

\examples{
if (rlang::is_installed(c("pins", "duckdb"))) {
  # Create a temporary board and pin some data
  board <- pins::board_temp()
  pins::pin_write(board, mtcars, "mtcars", type = "parquet")

  # Create a PinSource
  ps <- PinSource$new(board, "mtcars")

  # Query the pinned data
  ps$execute_query("SELECT * FROM mtcars WHERE mpg > 25")

  ps$cleanup()
}

}
\section{Super classes}{
\code{\link[querychat:DataSource]{DataSource}} -> \code{\link[querychat:DBISource]{DBISource}} -> \code{PinSource}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-PinSource-initialize}{\code{PinSource$new()}}
    \item \href{#method-PinSource-get_data_description}{\code{PinSource$get_data_description()}}
    \item \href{#method-PinSource-clone}{\code{PinSource$clone()}}
  }
}
\if{html}{\out{<details><summary>Inherited methods</summary>
<ul>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="cleanup"><a href='../../querychat/html/DBISource.html#method-DBISource-cleanup'><code>DBISource$cleanup()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="execute_query"><a href='../../querychat/html/DBISource.html#method-DBISource-execute_query'><code>DBISource$execute_query()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="get_data"><a href='../../querychat/html/DBISource.html#method-DBISource-get_data'><code>DBISource$get_data()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="get_db_type"><a href='../../querychat/html/DBISource.html#method-DBISource-get_db_type'><code>DBISource$get_db_type()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="get_schema"><a href='../../querychat/html/DBISource.html#method-DBISource-get_schema'><code>DBISource$get_schema()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="get_semantic_views_description"><a href='../../querychat/html/DBISource.html#method-DBISource-get_semantic_views_description'><code>DBISource$get_semantic_views_description()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="querychat" data-topic="DBISource" data-id="test_query"><a href='../../querychat/html/DBISource.html#method-DBISource-test_query'><code>DBISource$test_query()</code></a></span></li>
</ul>
</details>}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PinSource-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-PinSource-initialize}{}}}
\subsection{\code{PinSource$new()}}{
  Create a new PinSource
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{PinSource$new(
  board,
  name,
  ...,
  table_name = name,
  version = NULL,
  engine = getOption("querychat.DataFrameSource.engine", NULL)
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{board}}{A pins board object (e.g. from \code{\link[pins:board_folder]{pins::board_folder()}} or
\code{\link[pins:board_connect]{pins::board_connect()}}).}
      \item{\code{name}}{Name of the pin to read.}
      \item{\code{...}}{Not used; included for extensibility.}
      \item{\code{table_name}}{Name to use for the table in SQL queries. Defaults to
the pin name.}
      \item{\code{version}}{Pin version to read. If \code{NULL} (default), reads the
latest version.}
      \item{\code{engine}}{Database engine to use: \code{"duckdb"} or \code{"sqlite"}. Set the
global option \code{querychat.DataFrameSource.engine} to specify the default
engine. If \code{NULL} (default), uses the first available engine from
duckdb or RSQLite (in that order). Parquet, CSV, and JSON pins are read
most efficiently with the \code{"duckdb"} engine; with \code{"sqlite"} they are
deserialized via \code{pin_read()} instead.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new PinSource object
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PinSource-get_data_description"></a>}}
\if{latex}{\out{\hypertarget{method-PinSource-get_data_description}{}}}
\subsection{\code{PinSource$get_data_description()}}{
  Get a human-readable description of the pin for use in the system prompt.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{PinSource$get_data_description()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A string with the pin title, description, and tags, or an empty
string if none are set.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PinSource-clone"></a>}}
\if{latex}{\out{\hypertarget{method-PinSource-clone}{}}}
\subsection{\code{PinSource$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{PinSource$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}

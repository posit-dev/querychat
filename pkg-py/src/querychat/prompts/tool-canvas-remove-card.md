Remove a card from the user's dashboard canvas by `name`.

The card is hidden, not deleted — it stays in the results palette and can be
restored later via `querychat_canvas_arrange`. Unknown names return an error.
